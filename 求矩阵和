int sumMatrix(int a[][N]) {
	int i, j, sum = 0;
	for (i = 0; i < N; i++) {
		for (j = 0; j < N; j++) {
			if (j == N - 1 || i == N - 1 || i==j)
				;
			else
				sum = sum + a[i][j];
		}
	}
	return sum;
}
